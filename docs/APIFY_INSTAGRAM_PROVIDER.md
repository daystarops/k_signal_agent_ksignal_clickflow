# Apify Instagram Provider

Primary actor: `apify/instagram-scraper`; credential: `APIFY_TOKEN`. Missing credentials produce `down` / `AUTH_MISSING` and fail fast. Normalization retains caption, hashtags, mentions, visible metrics, owner/media metadata, and raw provider payload path. The provider never invokes fallback.

