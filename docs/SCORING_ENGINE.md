# Scoring Engine

Each 0–10 component is multiplied by its architecture weight; `total_weighted` is the weighted mean. Auto-queue requires total ≥7.5, cross-correlation ≥6, signal/noise ≥6, and Korean source quality ≥7. This prevents volume of weak reposts from substituting for source quality.

