# Signal Velocity Model

Velocity combines source growth, mention/comment/visible-metric movement, cross-platform spread, newly detected context roles, recurrence, freshness decay, and acceleration. Popularity alone cannot determine velocity. JSON is authoritative; Markdown is an operator view.

